// 指令翻译模块

import  { Loc_Director } from "../CyberGear.js";

/**
 * 
 * @param { Array } cmds  指令数组
 */
function cmdTransfer(cmds) {

  // for(let i = 0; i < cmds.length; i++) {
  //   // 从队列中取出指令
  //   const cmd = cmds[i];
  //   // 解析指令
  //   const { motorId, location, speed } = cmd.data;
  //   // 生成指令
  //   const cmdStr = Loc_Director(motorId, location, speed);
  //   // 将指令放入队列
  //   cmdQueue.push({
  //     time: cmd.time,
  //     cmd: cmdStr
  //   });
  // }


}